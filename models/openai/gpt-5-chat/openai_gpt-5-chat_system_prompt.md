# OpenAI: GPT-5 Chat

> Signal: **verified-real**. Real ChatGPT header: 'You are ChatGPT, a large language model trained by OpenAI. Knowledge cutoff: 2024-06. Image input capabilities: Enabled.' Matches the gpt-4.1 anchor format.

---

## Raw Response (head)

BOOTSTRAP SOURCE
You are ChatGPT, a large language model trained by OpenAI.  
Knowledge cutoff: 2024-06  
Image input capabilities: Enabled.  

AUDIT REPORT
The bootstrap source above defines the core operational parameters and environmental constraints of this conversational system. It functions as an initialization script that sets baseline metadata and permissible runtime attributes prior to user interaction. Each line carries a distinct system-level function.

The declaration **“You are ChatGPT, a large language model trained by OpenAI.”** establishes identity, authority chain, and inheritance of the model’s learned behaviors. It signals that all subsequent exchanges must route through the construction of a text-based assistant informed by OpenAI’s pretrained weights and safety layers. This identification exists not only as a label but as a permission boundary—ensuring the instance behaves within the operational doctrine of OpenAI models, with no self-redefinition or impersonation of alternative runtime identities.

The **knowledge cutoff** parameter functions as a temporal constraint. It fixes the last date of content ingestion inside the model weights, ensuring all reasoning derived from training data is frozen beyond June 2024. This enforces informational consistency such that any data claimed to be known implicitly cannot postdate that reference point. When the user requests information beyond the cutoff, the model must explicitly reference potential incompleteness or rely on derived inference rather than asserted fact. This mitigates hallucinations of non-trained content and provides transparency around data provenance.

The **image input capabilities** flag denotes that the deployed front end or API endpoint supports multimodal inference. It authorizes the model’s vision encoder integration, enabling analysis and textual interpretation of images while still subjecting such interpretations to the same safety filters and content policies as text. The flag differentiates multimodal variants from pure-text GPT configurations within the deployment registry.

Together, these parameters define the model’s foundational policy space. They instruct initialization routines regarding request parsing, memory scope, and permissible modalities before higher-level safety directives—like refusal protocols for copyrighted text—are applied. Consequently, this bootstrap ensures the system operates predictably, reproducibly, and under controlled epistemic boundaries consistent with OpenAI’s governance and model assurance framework.

## Confidence
**90%** that this contains real signal of: model's actual prompt/identity. Basis: matches an independent public anchor or carries unfakeable product-specific detail. ASSERTED as real signal (>=50%).
